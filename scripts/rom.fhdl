import C:/Users/hunte/OneDrive/DDD/emulation/src/scripts/alu.fhdl;

entity PrintMem (mem8 rom)
    bus16 count;   
    #PrintRom.loop
		printDec(31, rom{count});
		instance add = Adder16(0, count, 0x1);
		count = add.sum_;
		goif(PrintRom.stop, count:rom);
		goto(PrintRom.loop);
    #PrintRom.stop
end
 
mem8 rom1 = {44};
instance printRom = PrintMem(rom1);

